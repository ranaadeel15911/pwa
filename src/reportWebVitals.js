import { reportWebVitals } from 'web-vitals'; // Ensure this import is correct

// Function to log performance metrics (optional)
const reportWebVitals = (metric) => {
  console.log(metric);  // You can replace this with custom logging
};

export default reportWebVitals;
